on: push
name: 🚀 Subir site
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.1.0
      with:
        server: 162.215.208.119
        username: github@vkwp21.viskoo-cloud-server.com.br
        password: ${{ secrets.SENHA_PUBLIC }}
